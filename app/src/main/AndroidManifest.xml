<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/logo_the_ring_transparente_negro"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo_the_ring_transparente_negro"
        android:supportsRtl="true"
        android:theme="@style/Theme.TheRingPrivate">
        <activity
            android:name=".SettingsActivity"
            android:exported="false"
            android:theme="@style/TemaAjustesPremium" />
        <activity
            android:name=".HomeActivity"
            android:windowSoftInputMode="adjustResize"
            android:exported="false" />
        <activity
            android:name=".RegisterActivity"
            android:windowSoftInputMode="adjustResize"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:windowSoftInputMode="adjustResize"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PrivateChatActivity" android:exported="false" />
        <activity android:name=".GlobalChatActivity" android:exported="false" />
        <activity android:name=".PrivateChatListActivity" android:exported="false" />
    </application>

</manifest>